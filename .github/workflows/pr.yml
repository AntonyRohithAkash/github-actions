name: PR Verification
on: 
  pull_request:
    branches:
      - new_branch
jobs:
  Verification:
    runs-on: ubuntu-latest
    environment: test
    steps:
      -
        name: "Test"
        run: echo "RELEASE_VERSION"

# name: Release_Workflow
# on: 
#   push:
#     branches:
#       - main
# jobs:
#   Release_Job:
#     runs-on: ubuntu-latest
#     environment: test
#     steps:
#       - 
#         name: Check out repository code
#         uses: actions/checkout@v4
#       - 
#         name: Fetching GO version from repo
#         run: echo "GO_VERSION=1.22.6" >> $GITHUB_ENV
#       - 
#         name: Setup GO environment
#         uses: actions/setup-go@v5
#         with:
#           go-version: ${{ env.GO_VERSION }}
#       - 
#         name: Create Release Info
#         run: echo "RELEASE_VERSION=3.5.0" >> $GITHUB_ENV
#       - 
#         name: Setup git config for creating Tag
#         run: |
#           git config user.name "GitHub Actions"
#           git config user.email "<>"
#       - 
#         name: Update sdk version and create Tag
#         run: |
#           git tag ${{ env.RELEASE_VERSION }}
#           git push origin ${{ env.RELEASE_VERSION }}
#       -
#         name: Import GPG key
#         id: import_gpg
#         uses: crazy-max/ghaction-import-gpg@v6
#         with:
#           gpg_private_key: ${{ secrets.KEY }}
#           passphrase: "testing"
#       -
#         name: Run GoReleaser
#         uses: goreleaser/goreleaser-action@v6
#         with:
#           distribution: goreleaser
#           version: "~> v2"
#           args: release --clean
#         env:
#           GPG_FINGERPRINT: ${{ steps.import_gpg.outputs.fingerprint }}
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
